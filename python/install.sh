#!/bin/sh

# Not sure why I've created a virtualenv in the home directory but that seems
# like a bad idea and it's causing errors
# sudo -H pip install virtualenv
# virtualenv $HOME
# source $HOME/bin/activate

# pip install --upgrade setuptools pip

# pip install SpoofMAC
