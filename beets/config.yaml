library: ~/Music/library/library.db
directory: ~/Music/library/files
plugins: chroma discogs embedart fetchart importfeeds lastgenre

# Use English names if possible
languages:
  - en

# Use the album's original date instead of the release's date
original_date: yes

# Prevent iTunes from being an asshole and not importing some files
asciify_paths: yes
max_filename_length: 150

importfeeds:
  absolute_path: yes
  dir: ~/Music/library/imports
  formats: link

fetchart:
  sources: filesystem coverart discogs amazon albumart 

lastgenre:
  # auto: yes
  canonical: ~/.config/beets/canonical.txt
  # count: 1
  # fallback: ''
  force: no
  min_weight: 6
  # prefer_specific: no
  # separator: ', '
  # source: album
  whitelist: ~/.config/beets/whitelist.txt